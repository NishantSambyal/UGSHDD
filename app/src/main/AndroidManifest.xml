<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="technician.inteq.com.ugshdd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/UGS_HDD">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.Dashboard" />
        <activity android:name=".ui.activity.PendingCases" />
        <activity android:name=".ui.activity.DailyCashReport" />
        <activity android:name=".ui.activity.LeaveManagement" />
        <activity android:name=".ui.activity.StockManagementActivity" />
        <activity android:name=".ui.activity.DailyCashReportActivity" />
    </application>

</manifest>